"""Endpoint to receive and return event-levlel attribution reports."""
from importlib import import_module

helpers = import_module(
    "attribution-reporting.resources.reports-helpers")


def main(request, response):
  return helpers.handle_reports(request)
